<configuration>
    <!-- Console appender -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- fecha y hora, hilo de ejecucion, clase, metodo y mensaje -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    <!-- Set log levels -->
    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>
    <!-- DEBUG level para clases dentro de este paquete, cambiar en prod -->
    <logger name="edu.unam.fi.bdn.pf" level="DEBUG"/>
</configuration>